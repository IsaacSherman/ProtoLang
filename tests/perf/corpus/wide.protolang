// Generated by StressCorpus.Generate(). Do not edit by hand.
//
// The large half of the fixed corpus the performance budgets are measured against; the
// normal half is examples/simpleScript.protolang. See docs/performance.md for what each
// number means and StressCorpus for why this file is generated and committed rather than
// one or the other.

import proto "invoice.proto";

extend InvoiceItem {
    fn base_cents() -> int64 {
        return quantity * unit_price_cents;
    }

    fn step_0(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_1(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_2(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_3(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_4(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_5(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_6(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_7(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_8(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_9(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_10(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_11(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_12(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_13(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_14(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_15(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_16(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_17(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_18(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_19(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_20(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_21(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_22(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_23(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_24(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_25(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_26(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_27(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_28(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_29(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_30(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_31(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_32(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_33(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_34(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_35(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_36(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_37(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_38(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_39(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_40(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_41(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_42(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_43(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_44(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_45(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_46(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_47(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_48(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_49(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_50(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_51(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_52(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_53(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_54(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_55(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_56(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_57(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_58(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_59(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_60(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_61(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_62(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_63(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_64(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_65(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_66(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_67(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_68(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_69(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_70(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_71(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_72(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_73(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_74(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_75(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_76(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_77(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_78(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_79(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_80(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_81(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_82(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_83(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_84(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_85(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_86(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_87(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_88(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_89(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_90(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_91(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_92(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_93(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_94(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_95(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_96(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_97(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_98(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_99(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_100(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_101(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_102(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_103(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_104(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_105(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_106(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_107(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_108(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_109(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_110(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_111(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_112(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_113(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_114(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_115(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_116(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_117(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_118(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_119(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_120(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_121(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_122(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_123(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_124(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_125(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_126(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_127(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_128(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_129(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_130(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_131(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_132(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_133(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_134(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_135(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_136(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_137(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_138(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_139(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_140(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_141(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_142(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_143(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_144(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_145(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_146(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_147(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_148(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_149(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_150(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_151(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_152(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_153(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_154(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_155(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_156(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_157(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_158(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_159(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_160(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_161(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_162(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_163(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_164(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_165(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_166(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_167(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_168(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn step_169(factor: int64) -> int64 {
        var carried: int64 = base_cents();
        var taken: int64 = 0;

        if factor <= 0 {
            return carried;
        }

        while taken < factor {
            carried = carried + base_cents() * taken;
            taken = taken + 1;
        }

        return carried - base_cents();
    }

    fn deep_locals() -> int64 {
        var held_0: int64 = base_cents();
        var held_1: int64 = held_0 + base_cents();
        var held_2: int64 = held_1 + base_cents();
        var held_3: int64 = held_2 + base_cents();
        var held_4: int64 = held_3 + base_cents();
        var held_5: int64 = held_4 + base_cents();
        var held_6: int64 = held_5 + base_cents();
        var held_7: int64 = held_6 + base_cents();
        var held_8: int64 = held_7 + base_cents();
        var held_9: int64 = held_8 + base_cents();
        var held_10: int64 = held_9 + base_cents();
        var held_11: int64 = held_10 + base_cents();
        var held_12: int64 = held_11 + base_cents();
        var held_13: int64 = held_12 + base_cents();
        var held_14: int64 = held_13 + base_cents();
        var held_15: int64 = held_14 + base_cents();
        var held_16: int64 = held_15 + base_cents();
        var held_17: int64 = held_16 + base_cents();
        var held_18: int64 = held_17 + base_cents();
        var held_19: int64 = held_18 + base_cents();
        var held_20: int64 = held_19 + base_cents();
        var held_21: int64 = held_20 + base_cents();
        var held_22: int64 = held_21 + base_cents();
        var held_23: int64 = held_22 + base_cents();
        var held_24: int64 = held_23 + base_cents();
        var held_25: int64 = held_24 + base_cents();
        var held_26: int64 = held_25 + base_cents();
        var held_27: int64 = held_26 + base_cents();
        var held_28: int64 = held_27 + base_cents();
        var held_29: int64 = held_28 + base_cents();
        var held_30: int64 = held_29 + base_cents();
        var held_31: int64 = held_30 + base_cents();
        var held_32: int64 = held_31 + base_cents();
        var held_33: int64 = held_32 + base_cents();
        var held_34: int64 = held_33 + base_cents();
        var held_35: int64 = held_34 + base_cents();
        var held_36: int64 = held_35 + base_cents();
        var held_37: int64 = held_36 + base_cents();
        var held_38: int64 = held_37 + base_cents();
        var held_39: int64 = held_38 + base_cents();
        var held_40: int64 = held_39 + base_cents();
        var held_41: int64 = held_40 + base_cents();
        var held_42: int64 = held_41 + base_cents();
        var held_43: int64 = held_42 + base_cents();
        var held_44: int64 = held_43 + base_cents();
        var held_45: int64 = held_44 + base_cents();
        var held_46: int64 = held_45 + base_cents();
        var held_47: int64 = held_46 + base_cents();
        var held_48: int64 = held_47 + base_cents();
        var held_49: int64 = held_48 + base_cents();
        var held_50: int64 = held_49 + base_cents();
        var held_51: int64 = held_50 + base_cents();
        var held_52: int64 = held_51 + base_cents();
        var held_53: int64 = held_52 + base_cents();
        var held_54: int64 = held_53 + base_cents();
        var held_55: int64 = held_54 + base_cents();
        var held_56: int64 = held_55 + base_cents();
        var held_57: int64 = held_56 + base_cents();
        var held_58: int64 = held_57 + base_cents();
        var held_59: int64 = held_58 + base_cents();

        return held_59;
    }
}

extend Invoice {
    fn stepped_total_cents() -> int64 {
        var total: int64 = 0;

        for item in items {
            total = total + item.base_cents();
            total = total + item.step_0(3);
            total = total + item.step_169(5);
            total = total + item.deep_locals();
        }

        return total;
    }
}
